# PostgreSQL Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=ChangeThisSecurePassword123!
POSTGRES_DB=webportal

# Frontend Port
FRONTEND_PORT=80

# Backend configuration
PORT=3001
DB_HOST=postgres
DB_PORT=5432
DB_NAME=webportal
DB_USER=admin
DB_PASSWORD=Admin!1234

# Notes:
# 1. Copy this file to .env.production and update all passwords
# 2. Never commit .env.production to git
# 3. Use strong, unique passwords for production
# 4. Consider using Docker secrets for sensitive data in production
